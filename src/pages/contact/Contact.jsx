import React from 'react';

const Contact = () => {
  throw new Error('Error in Contact.jsx');

  // eslint-disable-next-line no-unreachable
  return (
    <div>This shouldn't be displayed</div>
  );
};

export default Contact;
